export type TypeLogin = {
    email: string,
    password: string,
};

export type TypeRegistration = {
    confirm: string,
    email: string,
    login: string,
    password: string,
    phone: string,
};
